/** Hours without a successful sync before an ACTIVE connection is considered stale for operational UX. */
export const STALE_SYNC_HOURS = 72;

/** Minimum connection age before alerting "never synced" (avoids noise right after link). */
export const NEVER_SYNCED_MIN_AGE_HOURS = 36;

export const RECENT_INFLOW_WINDOW_DAYS = 14;
