/**
 * @deprecated Removed from the request pipeline. Use `proxy.ts` at the project root
 * with `lib/security/proxy-policy.ts`, `lib/cors.ts`, and `lib/api/rate-limit-request.ts`.
 *
 * This file is retained only to avoid broken imports during migration.
 */
export {};
